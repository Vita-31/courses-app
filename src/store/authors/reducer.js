const initialState = []

export function authorsReducer(state = initialState, action) {
    switch(action.type) {
        
        default:
            return state
    }
}

export function authorsSelectors(state) {
    return state.user
}